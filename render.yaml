services:
  - type: web
    name: telegram-clone-1
    env: node
    plan: free
    buildCommand: |
      echo "Installing root dependencies..."
      npm install
      echo "Installing frontend dependencies..."
      cd frontend && npm install
      echo "Building frontend..."
      npm run build
      echo "Installing backend dependencies..."
      cd ../backend && npm install
      echo "Build completed successfully!"
    startCommand: cd backend && npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
      - key: MONGODB_URI
        value: mongodb+srv://tilavovazizbek37_db_user:aevOGZSOsR80bbAO@cluster0.uzczovb.mongodb.net/telegram-clone?retryWrites=true&w=majority
      - key: JWT_SECRET
        value: telegram-clone-secret-key-2024-render-production-super-secure
      - key: FRONTEND_URL
        value: https://telegram-clone-1.onrender.com